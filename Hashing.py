# Hashing 
class node :
    def __init__(self,data):
        self.value = data
        self.next = None
    def insert_end(self):
        tail.next=temp
        tail=temp
        return
    def search(self):
        curr
        
    def delete_middle(self):
        prev.next=curr.next
        return
    def hashing(k):
        for i in range(0,len(k)):
            H = k % 10
print(H)